[pytest]
# Plain `pytest` (unlike `python -m pytest`) does not add the current
# directory to sys.path on its own. Without this, tests/ can't import
# agents, core, services, or main - they'd only work when run via
# `python -m pytest`, which is not how the CI workflow invokes it.
pythonpath = .